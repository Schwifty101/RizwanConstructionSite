# Supabase Configuration (Required)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Environment
# Use production for deployments; development for local runs
NODE_ENV=production

# Optional: Site URL (set on hosting platform if needed)
SITE_URL=http://localhost:3000

# Optional: Admin email used for auth/notifications
# ADMIN_EMAIL=admin@example.com

# Optional: Redis for distributed rate limiting (recommended in production)
# REDIS_URL=redis://username:password@host:port

# Optional: Sentry DSN for error monitoring
# SENTRY_DSN=

# Optional: SMTP for contact form emails
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
